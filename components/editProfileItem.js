import react from "react";
import React, {useState} from react;
import { View, Text, StatusBar, Button, Alert, Modal} from 'react-native';

import styles from '../Styles.js'

export default class profileItemEdit(){
    return({

    });
}